jwt:
    secret: carrovivo-secret-key-2026-muito-seguro-aqui
    expiration: 86400000

encryption:
    secret: carrovivo-aes256-key-32chars!!!

spring:
    application:
        name: carrovivo-api

    datasource:
        url: jdbc:postgresql://localhost:5432/carrovivo
        username: carrovivo
        password: carrovivo123
        driver-class-name: org.postgresql.Driver

    jpa:
        hibernate:
            ddl-auto: validate
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                format_sql: true

    flyway:
        enabled: true
        locations: classpath:db/migration
        baseline-on-migrate: true

server:
    port: 8080

springdoc:
    api-docs:
        path: /api-docs
    swagger-ui:
        path: /swagger-ui.html